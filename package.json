{
  "name": "flashcards",
  "scripts": {
    "build": "npx tailwindcss -i ./src/main/resources/static/css/tailwind.css -o ./src/main/resources/static/css/tailwind.min.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "lit": "^3.3.2",
    "tailwindcss": "^3"
  }
}